plugins {
    id "java"
    id "maven-publish"
}

group "lol.pyr"
version "2.1.0"

repositories {
    mavenLocal()
    mavenCentral()
    maven {
        url "https://repo.papermc.io/repository/maven-public/"
    }
}

java {
    toolchain {
        languageVersion.set JavaLanguageVersion.of(21)
    }
}

dependencies {
    compileOnly "io.papermc.paper:paper-api:1.20.4-R0.1-SNAPSHOT"
    compileOnly "space.arim.dazzleconf:dazzleconf-yaml:2.0.0-M1-PYRBU"
}

java {
    withSourcesJar()
    withJavadocJar()
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
            artifactId = "simplergui"

            pom {
                name.set("simplergui")
            }
        }
    }
    repositories {
        mavenLocal()
    }
}